<<<<<<< HEAD
# Blockmate
BlockMate is a software that will help you start using your phone more consciously.
=======
# BlockMate - Боротьба з залежністю від соціальних мереж

Рішення для контролю використання соціальних мереж через Telegram-бота та AI-валідацію.

## 📋 Опис

BlockMate допомагає користувачам боротись з імпульсивним використанням соцмереж, пропонуючи AI-валідацію кожного запиту на відкриття соціальної мережі. Система аналізує запити користувача в контексті його цілей та дає рекомендації.

## 🏗️ Архітектура

- **Telegram Bot** - інтерфейс для взаємодії з користувачем
- **FastAPI Backend** - REST API для обробки запитів
- **MongoDB** - база даних для зберігання профілів користувачів
- **OpenAI API** - AI-валідація запитів
- **Docker** - контейнеризація для легкого деплою

## 🚀 Швидкий старт

### Вимоги

- Docker та Docker Compose
- Telegram Bot Token (отримайте у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (отримайте на [platform.openai.com](https://platform.openai.com))

### Встановлення

1. **Клонуйте репозиторій:**
```bash
git clone <repository-url>
cd BlockMate
```

2. **Створіть файл `.env` на основі `.env.example`:**
```bash
cp .env.example .env
```

3. **Заповніть `.env` файл:**
```env
TELEGRAM_BOT_TOKEN=ваш_telegram_bot_token
OPENAI_API_KEY=ваш_openai_api_key
MONGODB_URL=mongodb://mongodb:27017
MONGODB_DB_NAME=blockmate
BACKEND_URL=http://backend:8000
```

4. **Запустіть проект через Docker Compose:**
```bash
docker-compose up --build
```

Система запуститься на:
- Backend API: `http://localhost:8000`
- MongoDB: `mongodb://localhost:27017`
- Telegram Bot: автоматично підключиться до Telegram

### Локальний запуск (без Docker)

1. **Встановіть залежності:**
```bash
pip install -r requirements.txt
```

2. **Запустіть MongoDB локально** або використовуйте MongoDB Atlas

3. **Запустіть Backend:**
```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

4. **Запустіть Bot:**
```bash
python -m bot.main
```

## 📱 Налаштування iPhone Shortcut

Створіть Shortcut на iPhone для швидкого доступу до бота:

### Інструкція:

1. Відкрийте додаток **Shortcuts** на iPhone
2. Натисніть **"+"** для створення нового Shortcut
3. Додайте дії:
   - **"Open App"** → виберіть **Telegram**
   - **"Open URLs"** → додайте URL: `tg://resolve?domain=ваш_бот_username` (замініть на username вашого бота)
   - Або **"Send Message"** → виберіть Telegram та вашого бота

4. Назвіть Shortcut (наприклад, "BlockMate")
5. Додайте Shortcut на головний екран або використовуйте через Siri

### Альтернативний варіант через URL Scheme:

Можна використовувати URL для відкриття чату з ботом:
```
tg://resolve?domain=your_bot_username
```

Або для надсилання повідомлення:
```
tg://resolve?domain=your_bot_username&start=validate
```

## 💻 Використання

### Реєстрація та налаштування

1. Знайдіть свого бота в Telegram
2. Відправте `/start` для реєстрації
3. Використовуйте `/goals` для налаштування:
   ```
   Цілі: вивчити Python, розвивати блог
   Дозволені: навчання, робота, фітнес
   Заборонені: скрол, бездумні відео
   ```

### Валідація запитів

Коли ви хочете відкрити соцмережу:

1. Натисніть на Shortcut або відкрийте бота вручну
2. Відправте запит, наприклад:
   ```
   Хочу відкрити YouTube на 20 хв, щоб подивитися щось поки їм
   ```
3. Бот проаналізує запит та дасть рекомендацію:
   - ✅ **Дозволить** якщо це узгоджено з вашими цілями
   - ❌ **Не рекомендує** якщо це виглядає як відволікання

### Команди бота

- `/start` - реєстрація та початок роботи
- `/goals` - налаштування цілей та правил
- `/validate` - перевірка запиту
- `/help` - довідка

## 🔧 API Endpoints

### `POST /register_user`
Реєстрація нового користувача
```json
{
  "telegram_id": 123456,
  "username": "username"
}
```

### `POST /set_goals`
Встановлення цілей користувача
```json
{
  "telegram_id": 123456,
  "goals": ["learn python", "build a business"],
  "allowed_usecases": ["learning", "business research"],
  "forbidden_usecases": ["doomscrolling", "random shorts"]
}
```

### `POST /validate`
Валідація запиту через AI
```json
{
  "telegram_id": 123456,
  "request_text": "Хочу відкрити Instagram на 10 хвилин",
  "duration_minutes": 10
}
```

### `GET /user/{telegram_id}`
Отримання інформації про користувача

## 📊 Структура бази даних

### Колекція `users`:
```json
{
  "telegram_id": 123456,
  "username": "username",
  "goals": ["learn python"],
  "allowed_usecases": ["learning"],
  "forbidden_usecases": ["doomscrolling"],
  "history": [
    {
      "timestamp": "2024-01-01T12:00:00",
      "request": "open instagram",
      "decision": "deny",
      "alternative": "go for a walk"
    }
  ],
  "created_at": "2024-01-01T10:00:00"
}
```

## 🐳 Docker

Проект повністю контейнеризований:

```bash
# Запуск
docker-compose up

# Запуск у фоні
docker-compose up -d

# Зупинка
docker-compose down

# Перебудова
docker-compose up --build

# Перегляд логів
docker-compose logs -f bot
docker-compose logs -f backend
```

## 🔐 Безпека

- Не комітьте файл `.env` у репозиторій
- Використовуйте сильні API ключі
- Обмежте доступ до MongoDB в продакшені
- Розгляньте використання VPN або приватної мережі

## 🧪 Тестування

Запустіть скрипт для перевірки підключень:

```bash
python test_connection.py
```

Скрипт перевірить:
- Наявність змінних оточення
- Підключення до MongoDB
- Доступність Backend API

## 🛠️ Розробка

### Структура проекту:
```
BlockMate/
├── backend/
│   ├── main.py              # FastAPI додаток
│   ├── database.py          # MongoDB підключення
│   ├── models/
│   │   └── user.py          # Моделі даних
│   └── services/
│       └── openai_service.py # OpenAI інтеграція
├── bot/
│   └── main.py              # Telegram бот
├── docker-compose.yml
├── Dockerfile.backend
├── Dockerfile.bot
├── requirements.txt
└── README.md
```

### Додавання нових функцій

1. Backend: додайте нові endpoints у `backend/main.py`
2. Bot: додайте обробники у `bot/main.py`
3. Models: оновіть схему у `backend/models/user.py`

## 📝 Ліцензія

MIT License

## 🤝 Внесок

Вітаються pull requests та issues!

## 📧 Контакти

Якщо у вас є питання, створіть issue у репозиторії.

>>>>>>> e5c3271 (Initial project setup)
